# Migration documentation

Supporting materials that were previously stored inside `database/migrations/` have been relocated here to keep the migration namespace clean for `.sql` files only.

Current contents:
- `0015/` – Execution guides and audit notes for migration `0015_restore_all_missing_columns.sql`.
