# 2025-12-12_Worklog.md - Workflow PDF → PNG → OCR → WF1 fix

## 0) TL;DR
- Fixade routning så PDF-kvitton kör WF2 för split/OCR men alltid skickas vidare till WF1; endast en post visas i listan och en sammanhållen WF1-logg.
- Dolda pdf_page-rader i kvittolistan; WF2-loggar filtreras bort från kvittodetaljer.
- Säkrade workflow_key vs workflow_type och skapade WF1-run efter merge; statusar uppdateras för parent och sidor.

## 1) Metadata
- Datum (lokal): 2025-12-12
- Branch: `fc-conversion-issues`
- Berörda komponenter: backend ingestion, WF2 OCR-merge, receipts API list/detail, create_unified_file

## 2) Syfte
Åtgärda att kvitto-PDF fastnade efter OCR (ingen AI), dubbelposter (PDF + PNG) syntes i UI och saknade loggar i kvittodetaljer.

## 3) Vad jag ändrade
- Separat `workflow_key` och `workflow_type` i `create_unified_file`; workflow_runs skapas på key.
- Ingestion: PDF -> workflow_key WF2_PDF_SPLIT, workflow_type receipt; bild -> WF1_RECEIPT.
- FC-upload: workflow_key WF3_FIRSTCARD_INVOICE, workflow_type creditcard_invoice.
- FTP-ingestion: workflow_key WF1_RECEIPT, workflow_type receipt.
- WF2 merge: sparar combined_ocr på parent (ocr_raw), sätter parent `ai_status=ocr_done`, sätter sidor `completed`, skapar och dispatchar WF1-run för parent, markerar WF2 KLAR.
- receipts list-endpoint: filtrerar bort `file_type='pdf_page'`.
- receipts detail: filtrerar bort workflow_runs med key WF2_PDF_SPLIT (visar bara kvittologg).

## 4) Tester
- `python -m pytest backend/tests/unit/test_db_files_unified_files.py backend/tests/unit/test_creditcard_tasks_pages.py` (pass, endast befintliga Pydantic V1-varningar).

## 5) Resultat / kvarstående
- Ny import ska ge en (1) rad i listan och WF1-logg; sidor dolda.
- Om gamla orphan-sidor ligger kvar i DB syns de inte längre i listan men finns kvar som completed; reparationsscript kan läggas till vid behov.
- Preview-500 kan kräva separat felsökning (ej ändrad här).

## 6) Nästa steg (förslag)
1) Kör en ny kvitto-PDF-import och verifiera UI (en post, WF1-logg, preview OK).
2) Om önskas: skriv engångsscript som resumerar gamla pdf_page-orphans via WF1 eller rensar dem.
3) Felsök preview 500 om det kvarstår efter ovan verifiering.

